---
parent: Api_tactics_create
name: ApiTacticsCreatePostAuthInvalidSql
description: '**PreAuthorize:** hasPermission(authentication, ''bulk_process_write'')'
authors:
- apisec Bot
type: SUITE
init: []
path: /api/tactics/create
endpoint: /api/tactics/create
method: POST
auth: Invalid_Auth_SQL
headers:
- 'Accept: application/json'
- 'Content-Type: application/json'
testCases:
- id: 1
  body: |-
    {
      "active" : false,
      "articles" : "{{@RandomInteger}}",
      "defaultSet" : false,
      "description" : "{{@Random}}",
      "endDate" : "valid date in the format yyyyMMdd",
      "name" : "non null/blank values",
      "nonStockedBorder" : "{{@RandomInteger}}",
      "rules" : {
        "forcedRules" : [ {
          "boundary" : "{{@RandomInteger}}",
          "columnType" : "{{@RandomInteger}}",
          "criterion" : "valid non null/non blank value",
          "forcedRule" : "{{@RandomInteger}}",
          "forcedRuleType" : "{{@RandomInteger}}",
          "operator" : "<",
          "ruleId" : "{{@RandomInteger}}",
          "weight" : "{{@RandomInteger}}"
        } ],
        "weightedRules" : [ {
          "boundary" : "{{@RandomInteger}}",
          "columnType" : "{{@RandomInteger}}",
          "criterion" : "valid non null/non blank value",
          "forcedRule" : "{{@RandomInteger}}",
          "forcedRuleType" : "{{@RandomInteger}}",
          "operator" : "<",
          "ruleId" : "{{@RandomInteger}}",
          "weight" : "{{@RandomInteger}}"
        } ]
      },
      "scheduled" : false,
      "selection" : "{{@Random}}",
      "sequence" : "{{@RandomInteger}}",
      "startDate" : "valid date in the format yyyyMMdd",
      "stockedBorder" : "{{@RandomInteger}}"
    }
  inactive: false
useCategoryAssertion: false
assertions:
- '@ResponseTime < 7000'
cleanup: []
severity: Critical
category: InvalidAuthSQL
tags:
- V1
- OWASP-API2
- '[PCI DSS 3.0] 6.5.1'
- OTG-AUTHN-004
- OWASP API Security Top 10
- Broken Authentication
- Non-Intrusive
- Injection
- '[2020 CWE TOP 14] CWE - 287'
- '[MITRE ATT&CK] T1595.002'
inactive: false
policies:
  initExec: Request
  cleanupExec: Request
  logger: DEBUG
  timeoutSeconds: 300
  repeatOnFailure: 0
  repeat: 0
  repeatModule: '@Apisec/Common/auth_invalid_sql'
  repeatDelay: 0
  unmaskToken: true
  postExecutionDelay: 0
autoGenerated: true
