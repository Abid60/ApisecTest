---
parent: Api_tactics_insurance-inventory-setup
name: ApiTacticsInsuranceInventorySetupPostAuthInvalid
description: '**PreAuthorize:** hasPermission(authentication, ''bulk_process_write'')'
authors:
- apisec Bot
type: SUITE
init: []
path: /api/tactics/insurance-inventory-setup
endpoint: /api/tactics/insurance-inventory-setup
method: POST
auth: Invalid_Auth
headers:
- 'Accept: application/json'
- 'Content-Type: application/json'
testCases:
- id: 1
  body: |-
    {
      "active" : false,
      "articles" : "{{@RandomInteger}}",
      "defaultSet" : false,
      "defaultSetting" : "{{@RandomInteger}}",
      "description" : "{{@Random}}",
      "endDate" : "{{@Random}}",
      "groupBy1" : "{{@Random}}",
      "groupBy1Values" : [ "{{@Random}}" ],
      "groupBy2" : "{{@Random}}",
      "groupBy2Values" : [ "{{@Random}}" ],
      "insuranceInventoryDetails" : [ {
        "groupBy1Value" : "{{@Random}}",
        "groupBy2Value" : "{{@Random}}",
        "insuranceInventory" : "{{@RandomInteger}}"
      } ],
      "insuranceInventorySetting" : "{{@RandomInteger}}",
      "name" : "{{@Random}}",
      "onlyInsurance" : false,
      "scheduled" : false,
      "selection" : "{{@Random}}",
      "sequence" : "{{@RandomInteger}}",
      "startDate" : "{{@Random}}",
      "updateType" : "{{@RandomInteger}}"
    }
  inactive: false
useCategoryAssertion: false
assertions:
- '@StatusCode == 401 OR @StatusCode == 403'
cleanup: []
severity: Critical
category: InvalidAuth
tags:
- V1
- '[2020 CWE TOP 14] CWE - 287'
- OWASP-API2
- '[PCI DSS 3.0] 6.5.8'
- '[PCI DSS 3.0] 6.5.10'
- OTG-AUTHN-004
- OWASP API Security Top 10
- Broken Authentication
- Non-Intrusive
- '[MITRE ATT&CK] T1595.002'
inactive: false
policies:
  initExec: Request
  cleanupExec: Request
  logger: DEBUG
  timeoutSeconds: 300
  repeatOnFailure: 0
  repeat: 0
  repeatModule: null
  repeatDelay: 0
  unmaskToken: true
  postExecutionDelay: 0
autoGenerated: true
