---
parent: Api_supplier-order-groups_order-group
name: ApiSupplierOrderGroupsOrderGroupPatchDisallowedRbac
description: '**PreAuthorize:** hasPermission(authentication, ''order_write'')'
authors:
- apisec Bot
type: SUITE
init: []
path: /api/supplier-order-groups/order-group
endpoint: /api/supplier-order-groups/order-group
method: PATCH
auth: ROLE_system_write,ROLE_bulk_process_read,ROLE_extract_read,ROLE_promotions_write,ROLE_order_read,ROLE_promotions_read,ROLE_users_write,ROLE_parameters_write,ROLE_bulk_process_write,ROLE_selection_write,ROLE_selection_read,ROLE_users_read,ROLE_comment,ROLE_scheduler_write,ROLE_scheduler_read,ROLE_article_write,ROLE_article_read,ROLE_user_write,ROLE_extract_write,ROLE_system_read
headers:
- 'Accept: application/json'
- 'Content-Type: application/json'
testCases:
- id: 1
  body: |-
    {
      "acceptedToday" : false,
      "addPaymentTermsDays" : "{{@RandomInteger}}",
      "addedToSelectionGroup" : "{{@Random}}",
      "arg1" : "{{@Random}}",
      "arg2" : "{{@RandomInteger}}",
      "arg3" : "{{@Random}}",
      "calendarDetails" : [ {
        "distributeOrder" : "{{@RandomInteger}}",
        "noDelivery" : "{{@RandomInteger}}",
        "noOrder" : "{{@RandomInteger}}",
        "splitFactor" : "{{@RandomInteger}}",
        "weekNo" : "{{@RandomInteger}}",
        "yearNo" : "{{@RandomInteger}}"
      } ],
      "collectionGroup" : "{{@Random}}",
      "condition" : "{{@Random}}",
      "discountOnOrderPerc" : "{{@RandomInteger}}",
      "discountOnOrderValue" : "{{@RandomInteger}}",
      "discountProfile" : "{{@Random}}",
      "discounts" : {
        "basedOn" : "{{@Random}}",
        "data" : [ {
          "discountPerc" : "{{@RandomInteger}}",
          "rangeFrom" : "{{@RandomInteger}}"
        } ],
        "discountOn" : "{{@Random}}"
      },
      "exchangeRate" : "{{@RandomInteger}}",
      "exportName" : "{{@Random}}",
      "exportPath" : "{{@Random}}",
      "extractDateProfile" : "{{@Random}}",
      "frequency" : "{{@Random}}",
      "holidayCalendar" : "{{@Random}}",
      "lastOrderDate" : "{{@Random}}",
      "maxCrit1" : "{{@RandomInteger}}",
      "maxCrit2" : "{{@RandomInteger}}",
      "maxCrit3" : "{{@RandomInteger}}",
      "maxCrit4" : "{{@RandomInteger}}",
      "maxPurchaseValue" : "{{@RandomInteger}}",
      "minCrit1" : "{{@RandomInteger}}",
      "minCrit2" : "{{@RandomInteger}}",
      "minCrit3" : "{{@RandomInteger}}",
      "minCrit4" : "{{@RandomInteger}}",
      "minPurchaseValue" : "{{@RandomInteger}}",
      "orderScreen" : "{{@Random}}",
      "orderedToday" : false,
      "passwordProtected" : false,
      "processedToday" : false,
      "remark" : "{{@Random}}",
      "selectionCriterium" : "{{@Random}}",
      "selectionDescription" : "{{@Random}}",
      "selectionDetails" : [ {
        "addedToSelectionGroup" : "{{@Random}}",
        "supplierCode" : "{{@Random}}",
        "supplierName" : "{{@Random}}"
      } ],
      "selectionName" : "{{@Random}}",
      "sgCurrency" : "{{@Random}}",
      "startDateTime" : "{{@RandomInteger}}",
      "supplierCode" : "{{@Random}}",
      "supplierGroup" : [ "{{@Random}}" ],
      "supplierName" : "{{@Random}}",
      "users" : [ "{{@Random}}" ],
      "viewedToday" : false
    }
  inactive: false
useCategoryAssertion: false
assertions:
- '@StatusCode == 401 OR @StatusCode == 403'
cleanup: []
severity: High
category: RBAC
tags:
- V1
- OWASP-API5
- OWASP - OTG-IDENT-001
- OWASP API Security Top 10
- Broken Function Level Authorization
- Endpoint_Access_Control
- '[2020 CWE TOP 22 ] CWE - 269'
inactive: false
policies:
  initExec: Request
  cleanupExec: Request
  logger: DEBUG
  timeoutSeconds: 300
  repeatOnFailure: 0
  repeat: 0
  repeatModule: null
  repeatDelay: 0
  unmaskToken: false
  postExecutionDelay: 0
autoGenerated: true
