---
parent: Api_manual-curve-patterns_profile
name: ApiManualCurvePatternsProfilePostIncrementalIds
description: '**PreAuthorize:** hasPermission(authentication, ''bulk_process_write'')'
authors:
- apisec Bot
type: SUITE
init: []
path: /api/manual-curve-patterns/profile
endpoint: /api/manual-curve-patterns/profile
method: POST
auth: Default
headers:
- 'Accept: application/json'
- 'Content-Type: application/json'
testCases:
- id: 1
  body: |-
    {
      "countGeneric" : "{{@RandomInteger}}",
      "curveDescription" : "{{@Random}}",
      "curveProfile" : "{{@Random}}",
      "defaultProfile" : "{{@Random}}",
      "hasPassword" : false,
      "lastUpdateWeek" : "{{@Random}}",
      "locationType" : "{{@Random}}",
      "password" : "{{@PassAlphaNumSpl}}",
      "stringRange" : "{{@Random}}",
      "variantCurvesDetails" : [ {
        "ratio" : "{{@RandomInteger}}",
        "variantName" : "{{@Random}}",
        "variantNumber" : "{{@RandomInteger}}"
      } ]
    }
  inactive: false
useCategoryAssertion: false
assertions:
- '@Response.data.id != @NULL AND @Response.data.id != @Number'
- '@StatusCode == 200'
cleanup: []
severity: Medium
category: Incremental_Ids
tags:
- V1
- OWASP-API3
- CWE - 99
inactive: false
policies:
  initExec: Request
  cleanupExec: Request
  logger: DEBUG
  timeoutSeconds: 300
  repeatOnFailure: 0
  repeat: 0
  repeatModule: null
  repeatDelay: 0
  unmaskToken: false
  postExecutionDelay: 0
autoGenerated: true
