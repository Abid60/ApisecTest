---
parent: Api_selection
name: ApiSelectionPutExcessiveDataExposure
description: '**PreAuthorize:** hasPermission(authentication, ''selection_write'')'
authors:
- apisec Bot
type: SUITE
init: []
path: /api/selection
endpoint: /api/selection?originalProfile={{@Var.originalProfile}}
method: PUT
auth: Default
headers:
- 'Accept: application/json'
- 'Content-Type: application/json'
testCases:
- id: 1
  body: |-
    {
      "advancedSQL" : "{{@Random}}",
      "arg1" : "{{@Random}}",
      "arg2" : "{{@RandomInteger}}",
      "arg3" : "{{@Random}}",
      "articleField" : "{{@Random}}",
      "collectionAllUser" : false,
      "collectionsList" : [ ],
      "description" : "{{@Random}}",
      "frequency" : "{{@Random}}",
      "important" : false,
      "jsonString" : "{{@Random}}",
      "origin" : "{{@RandomInteger}}",
      "password" : "{{@PassAlphaNumSpl}}",
      "passwordProtected" : false,
      "profile" : "{{@Random}}",
      "pure" : "{{@Random}}",
      "rules" : [ {
        "criterium" : "{{@Random}}",
        "key" : "{{@Random}}",
        "operator" : "ends",
        "value" : { }
      } ],
      "selectionCategory" : "{{@Random}}",
      "selectionType" : "{{@RandomInteger}}",
      "startDateTime" : "{{@RandomInteger}}",
      "urgent" : false,
      "userGroup" : "{{@Random}}"
    }
  inactive: false
useCategoryAssertion: false
assertions:
- '@ResponseDepth <= 5'
cleanup: []
severity: Medium
category: Excessive_Data_Exposure
tags:
- V1
- OWASP-API3
- '[2020 CWE TOP 7] CWE - 200'
inactive: false
policies:
  initExec: Request
  cleanupExec: Request
  logger: DEBUG
  timeoutSeconds: 300
  repeatOnFailure: 0
  repeat: 0
  repeatModule: null
  repeatDelay: 0
  unmaskToken: false
  postExecutionDelay: 0
autoGenerated: true
